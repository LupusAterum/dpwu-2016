<Page
    x:Class="Cw6.EditTask"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cw6"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Content>
                <Grid>
                    
                
                    
                </Grid>
            </CommandBar.Content>
            <AppBarButton Icon="Accept" Label="accept" Click="confirmEdition"/>
            <AppBarButton Icon="Delete" Label="del entry" Click="deleteEntry"/>
            
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="1" VerticalAlignment="Center">Title:</TextBlock>
        <TextBox Grid.Column="2" Grid.Row="1" x:Name="taskTitle" Text="{Binding CurrentObject.Title, Mode=TwoWay}"></TextBox>
        <TextBlock Grid.Column="0" Grid.Row="3">Description:</TextBlock>
        <TextBox Grid.Column="2" Grid.Row="3" Height="200" x:Name="taskDesc" Text="{Binding CurrentObject.Value, Mode=TwoWay}" AcceptsReturn="True"></TextBox>
    </Grid>
</Page>
